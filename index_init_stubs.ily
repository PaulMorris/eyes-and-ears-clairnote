\version "2.19.63"

indexTitleString =
#(define-scheme-function (num alist) (number? list?)
   "")

indexComposerString =
#(define-scheme-function (num alist) (number? list?)
   "")

indexItem =
#(define-void-function (text) (markup?)
   '())
